alias emacs='emacsclient --tty'
alias gst='git status --short'

function mkcd() {
	mkdir -p "$@" && cd "$@"
}

function dx() {
    docker exec $@
}
compdef _docker dx="_docker_complete_running_containers"
