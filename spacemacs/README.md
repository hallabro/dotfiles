# Spacemacs (evil-mode) keybindings

Exit `insert` mode:
    `jk`

List snippets in helm:
    `Spc + i + s`

Expand current word (if snippet exists):
    `Meta + /` 

Save all files:
    `Spc + f + S`

Show keybindings:
    `Spc + ?`
